[ChoiceScript Docs](../README.md) › Arrays

# Arrays

ChoiceScript does not have native array types. Arrays are implemented as a set of numbered variables plus a count variable, all managed through dedicated commands.

## Pages

| Page | Description |
|---|---|
| [arrays.md](arrays.md) | How to declare, read, write, iterate, and dynamically resize arrays using `*create_array`, `*temp_array`, `*push`, `*pop`, and `*delete_array`. |
